export const defaultOptions = {
  radiusRatio: 0.4,
  marginRatio: 0.2,
  tooltip: {
    shared: true,
    showMarkers: false,
    offset: 20,
  },
  columnStyle: {
    lineWidth: 2,
  },
  interactions: [{ type: 'active-region' }],
};
